general {
        colors = true
        interval = 5
}

order += "memory"
order += "tztime local"

memory {
        format = "  %used "
        threshold_degraded = "1G"
        format_degraded = "MEMORY < %available"
}

tztime local {
        format = " %H:%M "
}
